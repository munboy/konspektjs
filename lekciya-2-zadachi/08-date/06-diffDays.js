// ──────────────────────────────────────────────────────────────────────
// Блок 8 · Задача 6 · Средне · ★ минимум
//
// Напиши `diffDays(a, b)` — сколько календарных дней от даты `a` до даты `b` (время внутри дня не
// важно; если `b` раньше — отрицательное число). Ответ не должен зависеть от летнего времени.
//
// `diffDays(new Date(2024, 0, 1), new Date(2024, 2, 1))` → `60`.
//
// Проверить себя:  node test.js diffDays
// Решение и разбор — в js-lekciya-2.html, блок 8, задача 6.
// ──────────────────────────────────────────────────────────────────────

function diffDays(a, b) {
  // твой код
}

// Примеры — запусти файл (node 08-date/06-diffDays.js) и сравни:
console.log(diffDays(new Date(2024, 0, 1), new Date(2024, 2, 1))); // ожидается: 60
console.log(diffDays(new Date(2024, 2, 30), new Date(2024, 3, 1))); // ожидается: 2
console.log(diffDays(new Date(2024, 0, 10, 23, 59), new Date(2024, 0, 11, 0, 1))); // ожидается: 1
