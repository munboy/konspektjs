// ──────────────────────────────────────────────────────────────────────
// Блок 8 · Задача 5 · Легко · ★ минимум
//
// Напиши `addDays(date, n)` — новую дату на `n` дней позже (или раньше, если `n` отрицательное).
// Исходную не менять.
//
// `addDays(new Date(2024, 0, 30), 3)` → 2 февраля 2024.
//
// Проверить себя:  node test.js addDays
// Решение и разбор — в js-lekciya-2.html, блок 8, задача 5.
// ──────────────────────────────────────────────────────────────────────

function addDays(date, n) {
  // твой код
}

// Примеры — запусти файл (node 08-date/05-addDays.js) и сравни:
console.log(addDays(new Date(2024, 0, 30), 3).getDate()); // ожидается: 2
