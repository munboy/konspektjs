// ──────────────────────────────────────────────────────────────────────
// Блок 9 · Задача 4 · Легко
//
// Напиши `pluralIntl(n, forms)` — то же, но через `Intl.PluralRules('uk')`: `forms` — объект `{
// one, few, many, other }` (`other` — для дробных).
//
// `pluralIntl(2, { one: 'яблуко', few: 'яблука', many: 'яблук', other: 'яблука' })` → `'2
// яблука'`.
//
// Проверить себя:  node test.js pluralIntl
// Решение и разбор — в js-lekciya-2.html, блок 9, задача 4.
// ──────────────────────────────────────────────────────────────────────

function pluralIntl(n, forms) {
  // твой код
}

// Примеры — запусти файл (node 09-intl/04-pluralIntl.js) и сравни:
console.log(pluralIntl(2, { one: 'яблуко', few: 'яблука', many: 'яблук', other: 'яблука' })); // ожидается: '2 яблука'
