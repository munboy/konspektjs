// ──────────────────────────────────────────────────────────────────────
// Блок 2 · Задача 12 · Сложно
//
// Напиши `deepCopy(value)` — глубокую копию: примитивы возвращаются как есть, массивы и обычные
// объекты копируются на всех уровнях, `Date` — новой датой с тем же временем.
//
// `deepCopy({ list: [1, { x: 2 }] })` — новый объект, и `list[1]` в нём — тоже новый объект.
//
// Проверить себя:  node test.js deepCopy
// Решение и разбор — в js-lekciya-2.html, блок 2, задача 12.
// ──────────────────────────────────────────────────────────────────────

function deepCopy(value) {
  // твой код
}

// Примеры — запусти файл (node 02-object/12-deepCopy.js) и сравни:
const src = { d: new Date(0), list: [1, { x: 2 }] };
const cp = deepCopy(src);
console.log(cp.list[1] !== src.list[1], cp.d.getTime()); // ожидается: true 0
