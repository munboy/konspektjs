// ──────────────────────────────────────────────────────────────────────
// Блок 2 · Задача 5 · Средне · ★ минимум
//
// Напиши `mapValues(obj, fn)` — новый объект с теми же ключами, где каждое значение заменено на
// `fn(value, key)`.
//
// `mapValues({ a: 1, b: 2 }, v => v * 10)` → `{ a: 10, b: 20 }`.
//
// Проверить себя:  node test.js mapValues
// Решение и разбор — в js-lekciya-2.html, блок 2, задача 5.
// ──────────────────────────────────────────────────────────────────────

function mapValues(obj, fn) {
  // твой код
}

// Примеры — запусти файл (node 02-object/05-mapValues.js) и сравни:
console.log(mapValues({ a: 1, b: 2 }, v => v * 10)); // ожидается: { a: 10, b: 20 }
console.log(mapValues({ x: 'k' }, (v, k) => k + v)); // ожидается: { x: 'xk' }
console.log(mapValues({}, v => v)); // ожидается: {}
