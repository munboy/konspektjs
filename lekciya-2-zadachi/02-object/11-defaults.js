// ──────────────────────────────────────────────────────────────────────
// Блок 2 · Задача 11 · Средне
//
// Напиши `defaults(target, ...sources)` — дописать в `target` значения из источников, но только
// для ключей, где в `target` сейчас `undefined`. Источники проверяются по порядку. Вернуть
// `target`.
//
// `defaults({ a: 1 }, { a: 2, b: 2 }, { b: 3, c: 3 })` → `{ a: 1, b: 2, c: 3 }`.
//
// Проверить себя:  node test.js defaults
// Решение и разбор — в js-lekciya-2.html, блок 2, задача 11.
// ──────────────────────────────────────────────────────────────────────

function defaults(target, ...sources) {
  // твой код
}

// Примеры — запусти файл (node 02-object/11-defaults.js) и сравни:
console.log(defaults({ a: 1 }, { a: 2, b: 2 }, { b: 3, c: 3 })); // ожидается: { a: 1, b: 2, c: 3 }
console.log(defaults({ a: undefined }, { a: 5 })); // ожидается: { a: 5 }
console.log(defaults({ a: null }, { a: 5 })); // ожидается: { a: null }
