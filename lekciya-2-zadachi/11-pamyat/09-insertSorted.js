// ──────────────────────────────────────────────────────────────────────
// Блок 11 · Задача 9 · Средне
//
// Напиши `insertSorted(arr, value)` — вставить число в уже отсортированный по возрастанию массив
// так, чтобы порядок сохранился (равные — после существующих). Место ищи двоичным поиском,
// вставляй через `splice`. Вернуть тот же массив.
//
// `insertSorted([1, 3, 5], 4)` → `[1, 3, 4, 5]`.
//
// Проверить себя:  node test.js insertSorted
// Решение и разбор — в js-lekciya-2.html, блок 11, задача 9.
// ──────────────────────────────────────────────────────────────────────

function insertSorted(arr, value) {
  // твой код
}

// Примеры — запусти файл (node 11-pamyat/09-insertSorted.js) и сравни:
console.log(insertSorted([1, 3, 5], 4)); // ожидается: [1, 3, 4, 5]
console.log(insertSorted([], 7)); // ожидается: [7]
console.log(insertSorted([1, 2], 0)); // ожидается: [0, 1, 2]
