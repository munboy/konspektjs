// ──────────────────────────────────────────────────────────────────────
// Блок 11 · Задача 6 · Сложно
//
// Напиши `deepCloneCycles(value)` — глубокую копию массивов и обычных объектов, которая
// выдерживает циклические ссылки: если объект уже копировался, вернуть его копию, а не копировать
// заново. Используй `WeakMap` «оригинал → копия».
//
// Проверить себя:  node test.js deepCloneCycles
// Решение и разбор — в js-lekciya-2.html, блок 11, задача 6.
// ──────────────────────────────────────────────────────────────────────

function deepCloneCycles(value, copies = new WeakMap()) {
  // твой код
}

// Примеры — запусти файл (node 11-pamyat/06-deepCloneCycles.js) и сравни:
const a = { name: 'A' };
a.self = a;
const b = deepCloneCycles(a);
console.log(b !== a, b.self === b); // ожидается: true true
