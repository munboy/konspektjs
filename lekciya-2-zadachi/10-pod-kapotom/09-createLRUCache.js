// ──────────────────────────────────────────────────────────────────────
// Блок 10 · Задача 9 · Средне
//
// Напиши `createLRUCache(capacity)` — кеш на `capacity` записей: `get(key)` (или `undefined`) и
// `set(key, value)`. Если места нет, выбрасывается запись, к которой дольше всего не обращались (и
// `get`, и `set` считаются обращением). Используй то, что `Map` хранит порядок добавления.
//
// Проверить себя:  node test.js createLRUCache
// Решение и разбор — в js-lekciya-2.html, блок 10, задача 9.
// ──────────────────────────────────────────────────────────────────────

function createLRUCache(capacity) {
  // твой код
}

