// ──────────────────────────────────────────────────────────────────────
// Блок 10 · Задача 2 · Средне · ★ минимум
//
// Напиши `createDynamicArray()` — свой «массив с ёмкостью», как в V8. Внутри — хранилище
// фиксированного размера. Методы: `push(value)` (вернуть новую длину), `get(i)`; свойства
// `length`, `capacity` (размер хранилища) и `copies` (сколько элементов пришлось переписать при
// всех расширениях). Когда место кончилось, новое хранилище — размером `n + n/2 + 16` (округление
// вниз), где `n` — новая длина.
//
// После первого `push` ёмкость 17, после 18-го — 43.
//
// Проверить себя:  node test.js createDynamicArray
// Решение и разбор — в js-lekciya-2.html, блок 10, задача 2.
// ──────────────────────────────────────────────────────────────────────

function createDynamicArray() {
  // твой код
}

// Примеры — запусти файл (node 10-pod-kapotom/02-createDynamicArray.js) и сравни:
const arr = createDynamicArray();
arr.push('a');
console.log(arr.length, arr.capacity); // ожидается: 1 17
