// ──────────────────────────────────────────────────────────────────────
// Блок 10 · Задача 5 · Легко · ★ минимум
// Правила: не больше одного цикла (forEach, map, slice, spread — это тоже циклы)
//
// Напиши `hashString(str, size)` — номер корзины от 0 до `size - 1`: полиномиальный хеш `h = h *
// 31 + код символа`, после каждого шага — остаток от деления на `size`.
//
// `hashString('abc', 16)` → `2`.
//
// Проверить себя:  node test.js hashString
// Решение и разбор — в js-lekciya-2.html, блок 10, задача 5.
// ──────────────────────────────────────────────────────────────────────

function hashString(str, size) {
  // твой код
}

// Примеры — запусти файл (node 10-pod-kapotom/05-hashString.js) и сравни:
console.log(hashString('abc', 16)); // ожидается: 2
console.log(hashString('', 16)); // ожидается: 0
console.log(hashString('abc', 16) === hashString('abc', 16)); // ожидается: true
