// ──────────────────────────────────────────────────────────────────────
// Блок 10 · Задача 8 · Средне
//
// Напиши `uniqueBy(arr, keyFn)` — оставить только первый элемент для каждого значения
// `keyFn(элемент)`. Один проход.
//
// `uniqueBy([{ id: 1, n: 'a' }, { id: 2, n: 'b' }, { id: 1, n: 'c' }], x => x.id)` → первые два
// объекта.
//
// Проверить себя:  node test.js uniqueBy
// Решение и разбор — в js-lekciya-2.html, блок 10, задача 8.
// ──────────────────────────────────────────────────────────────────────

function uniqueBy(arr, keyFn) {
  // твой код
}

// Примеры — запусти файл (node 10-pod-kapotom/08-uniqueBy.js) и сравни:
console.log(uniqueBy(['apple', 'avocado', 'banana'], w => w[0])); // ожидается: ['apple', 'banana']
console.log(uniqueBy([], x => x)); // ожидается: []
