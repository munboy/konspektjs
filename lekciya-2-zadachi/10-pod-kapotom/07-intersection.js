// ──────────────────────────────────────────────────────────────────────
// Блок 10 · Задача 7 · Легко
//
// Напиши `intersection(a, b)` — элементы массива `a`, которые есть в `b`, без повторов, в порядке
// `a`. Сделай это за O(n + m), а не вложенным перебором.
//
// `intersection([1, 2, 2, 3], [2, 3, 4])` → `[2, 3]`.
//
// Проверить себя:  node test.js intersection
// Решение и разбор — в js-lekciya-2.html, блок 10, задача 7.
// ──────────────────────────────────────────────────────────────────────

function intersection(a, b) {
  // твой код
}

// Примеры — запусти файл (node 10-pod-kapotom/07-intersection.js) и сравни:
console.log(intersection([1, 2, 2, 3], [2, 3, 4])); // ожидается: [2, 3]
console.log(intersection([], [1])); // ожидается: []
console.log(intersection([NaN, 1], [NaN])); // ожидается: [NaN]
