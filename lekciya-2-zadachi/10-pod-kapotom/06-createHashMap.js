// ──────────────────────────────────────────────────────────────────────
// Блок 10 · Задача 6 · Сложно · ★ минимум
//
// Напиши `createHashMap()` — свой `Map` на хеш-таблице, устроенный как в V8: записи в массиве в
// порядке добавления, корзины хранят номера записей, удаление оставляет «дырку», при переполнении
// — вдвое больше корзин и перестройка без дырок. Методы `set` (возвращает сам объект), `get`,
// `has`, `delete` (true/false), свойство `size`, метод `keys()` — массив ключей по порядку. Ключи
// сравниваются по SameValueZero; `1` и `'1'` — разные ключи.
//
// Проверить себя:  node test.js createHashMap
// Решение и разбор — в js-lekciya-2.html, блок 10, задача 6.
// ──────────────────────────────────────────────────────────────────────

function createHashMap() {
  // твой код
}

// Примеры — запусти файл (node 10-pod-kapotom/06-createHashMap.js) и сравни:
const map = createHashMap().set('a', 1).set('b', 2);
console.log(map.get('b'), map.size, map.keys()); // ожидается: 2 2 [ 'a', 'b' ]
