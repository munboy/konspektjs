// ──────────────────────────────────────────────────────────────────────
// Блок 3 · Задача 3 · Средне
//
// Напиши `moveItem(arr, from, to)` — переставить элемент с позиции `from` на позицию `to` в том же
// массиве и вернуть его.
//
// `moveItem(['a', 'b', 'c', 'd'], 0, 2)` → `['b', 'c', 'a', 'd']`.
//
// Проверить себя:  node test.js moveItem
// Решение и разбор — в js-lekciya-2.html, блок 3, задача 3.
// ──────────────────────────────────────────────────────────────────────

function moveItem(arr, from, to) {
  // твой код
}

// Примеры — запусти файл (node 03-array/03-moveItem.js) и сравни:
console.log(moveItem(['a', 'b', 'c', 'd'], 0, 2)); // ожидается: ['b', 'c', 'a', 'd']
console.log(moveItem([1, 2, 3], 2, 0)); // ожидается: [3, 1, 2]
console.log(moveItem([1, 2, 3], 1, 1)); // ожидается: [1, 2, 3]
