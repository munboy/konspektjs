// ──────────────────────────────────────────────────────────────────────
// Блок 3 · Задача 15 · Средне
//
// Напиши `partition(arr, predicate)` — разделить массив на два: `[подходящие, остальные]`, порядок
// сохраняется. Один проход.
//
// `partition([1, 2, 3, 4], x => x % 2 === 0)` → `[[2, 4], [1, 3]]`.
//
// Проверить себя:  node test.js partition
// Решение и разбор — в js-lekciya-2.html, блок 3, задача 15.
// ──────────────────────────────────────────────────────────────────────

function partition(arr, predicate) {
  // твой код
}

// Примеры — запусти файл (node 03-array/15-partition.js) и сравни:
console.log(partition([1, 2, 3, 4], x => x % 2 === 0)); // ожидается: [[2, 4], [1, 3]]
console.log(partition([], Boolean)); // ожидается: [[], []]
console.log(partition([0, 'a', null], Boolean)); // ожидается: [['a'], [0, null]]
