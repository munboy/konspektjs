// ──────────────────────────────────────────────────────────────────────
// Блок 3 · Задача 9 · Легко · ★ минимум
//
// Напиши `range(start, end, step = 1)` — числа от `start` до `end` (не включая) с шагом `step`;
// шаг может быть отрицательным. Используй `Array.from`.
//
// `range(0, 5)` → `[0, 1, 2, 3, 4]`, `range(5, 0, -2)` → `[5, 3, 1]`.
//
// Проверить себя:  node test.js range
// Решение и разбор — в js-lekciya-2.html, блок 3, задача 9.
// ──────────────────────────────────────────────────────────────────────

function range(start, end, step = 1) {
  // твой код
}

// Примеры — запусти файл (node 03-array/09-range.js) и сравни:
console.log(range(0, 5)); // ожидается: [0, 1, 2, 3, 4]
console.log(range(1, 10, 3)); // ожидается: [1, 4, 7]
console.log(range(5, 0, -2)); // ожидается: [5, 3, 1]
