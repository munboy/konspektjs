// ──────────────────────────────────────────────────────────────────────
// Блок 3 · Задача 2 · Легко
//
// Напиши `removeAt(arr, index)` — новый массив без элемента с индексом `index` (отрицательный — с
// конца). Исходный массив не менять. Индекс за границами — просто копия.
//
// `removeAt([1, 2, 3], 1)` → `[1, 3]`, `removeAt([1, 2, 3], -1)` → `[1, 2]`.
//
// Проверить себя:  node test.js removeAt
// Решение и разбор — в js-lekciya-2.html, блок 3, задача 2.
// ──────────────────────────────────────────────────────────────────────

function removeAt(arr, index) {
  // твой код
}

// Примеры — запусти файл (node 03-array/02-removeAt.js) и сравни:
console.log(removeAt([1, 2, 3], 1)); // ожидается: [1, 3]
