// ──────────────────────────────────────────────────────────────────────
// Блок 3 · Задача 11 · Средне
//
// Напиши `matrix(rows, cols, value)` — таблицу `rows × cols`, заполненную `value`, в которой
// строки — разные массивы.
//
// `matrix(2, 3, 0)` → `[[0, 0, 0], [0, 0, 0]]`.
//
// Проверить себя:  node test.js matrix
// Решение и разбор — в js-lekciya-2.html, блок 3, задача 11.
// ──────────────────────────────────────────────────────────────────────

function matrix(rows, cols, value) {
  // твой код
}

// Примеры — запусти файл (node 03-array/11-matrix.js) и сравни:
console.log(matrix(2, 3, 0)); // ожидается: [[0, 0, 0], [0, 0, 0]]
