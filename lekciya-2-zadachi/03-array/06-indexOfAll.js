// ──────────────────────────────────────────────────────────────────────
// Блок 3 · Задача 6 · Легко
//
// Напиши `indexOfAll(arr, value)` — все индексы, где стоит `value` (сравнение `===`). Используй
// `indexOf` со вторым аргументом — «искать начиная с».
//
// `indexOfAll([1, 2, 1, 3, 1], 1)` → `[0, 2, 4]`.
//
// Проверить себя:  node test.js indexOfAll
// Решение и разбор — в js-lekciya-2.html, блок 3, задача 6.
// ──────────────────────────────────────────────────────────────────────

function indexOfAll(arr, value) {
  // твой код
}

// Примеры — запусти файл (node 03-array/06-indexOfAll.js) и сравни:
console.log(indexOfAll([1, 2, 1, 3, 1], 1)); // ожидается: [0, 2, 4]
console.log(indexOfAll([], 1)); // ожидается: []
console.log(indexOfAll(['a', 'b'], 'b')); // ожидается: [1]
