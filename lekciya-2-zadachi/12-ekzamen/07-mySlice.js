// ──────────────────────────────────────────────────────────────────────
// Блок 12 · Задача 7 · Средне · ★ минимум
// Правила: не больше одного цикла (forEach, map, slice, spread — это тоже циклы); нельзя вызывать: slice, splice, concat
//
// Напиши `mySlice(arr, start, end)` — копию `arr.slice(start, end)`: `start` по умолчанию 0, `end`
// — длина; отрицательные — от конца; дробные отбрасываются, `NaN` — 0; выход за границы
// обрезается; дырки сохраняются. Один цикл.
//
// `mySlice([1, 2, 3, 4], 1, 3)` → `[2, 3]`, `mySlice([1, 2, 3], -2)` → `[2, 3]`.
//
// Проверить себя:  node test.js mySlice
// Решение и разбор — в js-lekciya-2.html, блок 12, задача 7.
// ──────────────────────────────────────────────────────────────────────

function mySlice(arr, start, end) {
  // твой код
}

// Примеры — запусти файл (node 12-ekzamen/07-mySlice.js) и сравни:
console.log(mySlice([1, 2, 3, 4], 1, 3)); // ожидается: [2, 3]
