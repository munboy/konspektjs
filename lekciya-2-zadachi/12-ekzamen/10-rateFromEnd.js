// ──────────────────────────────────────────────────────────────────────
// Блок 12 · Задача 10 · Сложно · ★ минимум · экзамен
// Правила: ровно один обратный цикл: for (let i = str.length - 1; i >= 0; i--); без массивов
//
// Смесь двух задач прошлого экзамена: читая строку справа налево (ровно один обратный `for`),
// найди последние два числа и верни курс `последнее / предпоследнее`. Дробная часть — через точку
// или запятую. Меньше двух чисел — `null`. Без `parseFloat`, `Number()` и массивов.
//
// `rateFromEnd('Exchanged 250 USD to 23125 CZK')` → `92.5`, `rateFromEnd('Rate 7 then 2,5 to 10')`
// → `4`.
//
// Проверить себя:  node test.js rateFromEnd
// Решение и разбор — в js-lekciya-2.html, блок 12, задача 10.
// ──────────────────────────────────────────────────────────────────────

function rateFromEnd(str) {
  // твой код
}

// Примеры — запусти файл (node 12-ekzamen/10-rateFromEnd.js) и сравни:
console.log(rateFromEnd('Exchanged 250 USD to 23125 CZK')); // ожидается: 92.5
console.log(rateFromEnd('Converted 3.5 BTC to 196000 USD')); // ожидается: 56000
console.log(rateFromEnd('Swapped 1000 EUR to 1085.50 USD')); // ожидается: 1.0855
