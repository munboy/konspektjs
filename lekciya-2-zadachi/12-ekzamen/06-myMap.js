// ──────────────────────────────────────────────────────────────────────
// Блок 12 · Задача 6 · Средне
// Правила: не больше одного цикла (forEach, map, slice, spread — это тоже циклы); нельзя вызывать: map, forEach, flatMap
//
// Напиши `myMap(arr, fn)` — копию `arr.map(fn)`: новый массив той же длины, дырки остаются дырками
// (функция для них не вызывается). Один цикл.
//
// Проверить себя:  node test.js myMap
// Решение и разбор — в js-lekciya-2.html, блок 12, задача 6.
// ──────────────────────────────────────────────────────────────────────

function myMap(arr, fn) {
  // твой код
}

// Примеры — запусти файл (node 12-ekzamen/06-myMap.js) и сравни:
console.log(myMap([1, 2, 3], x => x * 2)); // ожидается: [2, 4, 6]
console.log(myMap(['a', 'b'], (x, i) => x + i)); // ожидается: ['a0', 'b1']
console.log(myMap([1, , 3], x => x * 10).length); // ожидается: 3
