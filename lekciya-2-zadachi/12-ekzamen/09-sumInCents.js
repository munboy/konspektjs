// ──────────────────────────────────────────────────────────────────────
// Блок 12 · Задача 9 · Средне · ★ минимум · экзамен
// Правила: ровно один цикл for; без массивов
//
// Напиши `sumInCents(str)` — сумму всех денежных сумм в строке в целых копейках (центах), чтобы не
// было ошибок округления. Дробная часть — через точку или запятую (разделитель — только если после
// него цифра), цифры после второй дробной отбрасываются. Один `for`, `charCodeAt`, без
// `parseFloat` и массивов.
//
// `sumInCents('12.5 and 0.1 and 0.2')` → `1280`.
//
// Проверить себя:  node test.js sumInCents
// Решение и разбор — в js-lekciya-2.html, блок 12, задача 9.
// ──────────────────────────────────────────────────────────────────────

function sumInCents(str) {
  // твой код
}

// Примеры — запусти файл (node 12-ekzamen/09-sumInCents.js) и сравни:
console.log(sumInCents('12.5 and 0.1 and 0.2')); // ожидается: 1280
console.log(sumInCents('Paid 10,99 USDT')); // ожидается: 1099
console.log(sumInCents('7. End')); // ожидается: 700
