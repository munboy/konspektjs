// ──────────────────────────────────────────────────────────────────────
// Блок 12 · Задача 5 · Легко
// Правила: не больше одного цикла (forEach, map, slice, spread — это тоже циклы); нельзя вызывать: every, some, find, findIndex
//
// Напиши `myEvery(arr, fn)` — копию `arr.every(fn)`: дырки пропускаются, `fn(элемент, индекс,
// массив)`, выход на первом «нет», пустой массив — `true`. Один цикл.
//
// Проверить себя:  node test.js myEvery
// Решение и разбор — в js-lekciya-2.html, блок 12, задача 5.
// ──────────────────────────────────────────────────────────────────────

function myEvery(arr, fn) {
  // твой код
}

// Примеры — запусти файл (node 12-ekzamen/05-myEvery.js) и сравни:
console.log(myEvery([2, 4], x => x % 2 === 0)); // ожидается: true
console.log(myEvery([2, 3], x => x % 2 === 0)); // ожидается: false
console.log(myEvery([], () => false)); // ожидается: true
