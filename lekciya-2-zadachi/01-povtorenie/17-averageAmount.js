// ──────────────────────────────────────────────────────────────────────
// Блок 1 · Задача 17 · Легко
// Правила: только циклы, индексы str[i], charCodeAt и арифметика — без готовых методов, parseInt и Number(); ровно один цикл for; без массивов
//
// Напиши `averageAmount(str)` — среднее всех чисел строки (дробь через точку или запятую,
// разделитель — только если после него цифра). Чисел нет — `null`. Один `for`, без массивов.
//
// `averageAmount('Paid 10 and 20.5')` → `15.25`.
//
// Проверить себя:  node test.js averageAmount
// Решение и разбор — в js-lekciya-2.html, блок 1, задача 17.
// ──────────────────────────────────────────────────────────────────────

function averageAmount(str) {
  // твой код
}

// Примеры — запусти файл (node 01-povtorenie/17-averageAmount.js) и сравни:
console.log(averageAmount('Paid 10 and 20.5')); // ожидается: 15.25
console.log(averageAmount('a 1, b 2, c 3')); // ожидается: 2
console.log(averageAmount('3')); // ожидается: 3
