// ──────────────────────────────────────────────────────────────────────
// Блок 1 · Задача 13 · Сложно
// Правила: только циклы, индексы str[i], charCodeAt и арифметика — без готовых методов, parseInt и Number()
//
// Напиши `lastFloat(str)` — последнее дробное число в строке, читая её справа налево. Разделитель
// — точка, минус прямо перед числом — знак. Точка без цифр слева или справа допустима (`'.5'`,
// `'3.'`), вторая точка с конца завершает число. Чисел нет — `null`.
//
// `lastFloat('Total: 45.99')` → `45.99`, `lastFloat('Price -3.5')` → `-3.5`, `lastFloat('1.2.3')`
// → `2.3`.
//
// Проверить себя:  node test.js lastFloat
// Решение и разбор — в js-lekciya-2.html, блок 1, задача 13.
// ──────────────────────────────────────────────────────────────────────

function lastFloat(str) {
  // твой код
}

// Примеры — запусти файл (node 01-povtorenie/13-lastFloat.js) и сравни:
console.log(lastFloat('Total: 45.99')); // ожидается: 45.99
console.log(lastFloat('Price -3.5')); // ожидается: -3.5
console.log(lastFloat('a 1.5 b 2.25')); // ожидается: 2.25
