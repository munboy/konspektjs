// ──────────────────────────────────────────────────────────────────────
// Блок 1 · Задача 9 · Сложно · ★ минимум
// Правила: только циклы, индексы str[i], charCodeAt и арифметика — без готовых методов, parseInt и Number(); ровно один цикл for
//
// Напиши `allAmounts(message)` — массив всех чисел сообщения: минус прямо перед числом — знак;
// запятая или точка, после которой цифра, — десятичный разделитель (только первый в числе). Один
// `for`.
//
// `allAmounts('Paid 12,50 and 3.25, got -1')` → `[12.5, 3.25, -1]`.
//
// Проверить себя:  node test.js allAmounts
// Решение и разбор — в js-lekciya-2.html, блок 1, задача 9.
// ──────────────────────────────────────────────────────────────────────

function allAmounts(message) {
  // твой код
}

// Примеры — запусти файл (node 01-povtorenie/09-allAmounts.js) и сравни:
console.log(allAmounts('Paid 12,50 and 3.25, got -1')); // ожидается: [12.5, 3.25, -1]
console.log(allAmounts('')); // ожидается: []
console.log(allAmounts('1, 2, 3')); // ожидается: [1, 2, 3]
