// ──────────────────────────────────────────────────────────────────────
// Блок 1 · Задача 18 · Средне
// Правила: только циклы, индексы str[i], charCodeAt и арифметика — без готовых методов, parseInt и Number(); ровно один цикл for; без массивов
//
// Напиши `balanceSpread(str)` — разницу между наибольшим и наименьшим числом строки (дробь через
// точку или запятую). Чисел нет — `null`. Один `for`, без массивов.
//
// `balanceSpread('Min 12.5, max 40')` → `27.5`.
//
// Проверить себя:  node test.js balanceSpread
// Решение и разбор — в js-lekciya-2.html, блок 1, задача 18.
// ──────────────────────────────────────────────────────────────────────

function balanceSpread(str) {
  // твой код
}

// Примеры — запусти файл (node 01-povtorenie/18-balanceSpread.js) и сравни:
console.log(balanceSpread('Min 12.5, max 40')); // ожидается: 27.5
console.log(balanceSpread('a 3,5 b 1,25')); // ожидается: 2.25
console.log(balanceSpread('only 7')); // ожидается: 0
