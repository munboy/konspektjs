// ──────────────────────────────────────────────────────────────────────
// Блок 1 · Задача 19 · Средне
// Правила: только циклы, индексы str[i], charCodeAt и арифметика — без готовых методов, parseInt и Number(); ровно один цикл for; без массивов
//
// Зеркальная версия `contactNumber`: читаем слева направо. Первое число — начало результата (с
// дробной частью через точку или запятую, если есть), цифры всех следующих чисел дописываются в
// конец. Один обычный `for`, без массивов. Чисел нет — `null`.
//
// `glueFromLeft('Left 56,02 then 15 and 49')` → `56.021549`, `glueFromLeft('a 12 b 34')` → `1234`.
//
// Проверить себя:  node test.js glueFromLeft
// Решение и разбор — в js-lekciya-2.html, блок 1, задача 19.
// ──────────────────────────────────────────────────────────────────────

function glueFromLeft(str) {
  // твой код
}

// Примеры — запусти файл (node 01-povtorenie/19-glueFromLeft.js) и сравни:
console.log(glueFromLeft('Left 56,02 then 15 and 49')); // ожидается: 56.021549
console.log(glueFromLeft('a 12 b 34')); // ожидается: 1234
console.log(glueFromLeft('x 0.5 y 7')); // ожидается: 0.57
