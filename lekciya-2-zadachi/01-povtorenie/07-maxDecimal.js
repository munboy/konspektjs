// ──────────────────────────────────────────────────────────────────────
// Блок 1 · Задача 7 · Средне · ★ минимум
// Правила: только циклы, индексы str[i], charCodeAt и арифметика — без готовых методов, parseInt и Number()
//
// Напиши `maxDecimal(str)` — наибольшее число в тексте. Числа — цифры с необязательной дробной
// частью через точку (точка — часть числа, только если после неё цифра). Минусов нет. Чисел нет —
// `null`.
//
// `maxDecimal('a 1.5 b 12.25 c 3')` → `12.25`.
//
// Проверить себя:  node test.js maxDecimal
// Решение и разбор — в js-lekciya-2.html, блок 1, задача 7.
// ──────────────────────────────────────────────────────────────────────

function maxDecimal(str) {
  // твой код
}

// Примеры — запусти файл (node 01-povtorenie/07-maxDecimal.js) и сравни:
console.log(maxDecimal('a 1.5 b 12.25 c 3')); // ожидается: 12.25
console.log(maxDecimal('none')); // ожидается: null
console.log(maxDecimal('0.5 0.25')); // ожидается: 0.5
