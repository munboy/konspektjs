// ──────────────────────────────────────────────────────────────────────
// Блок 4 · Задача 4 · Легко · ★ минимум
//
// Что выведет этот код?
//
// Сначала подумай и впиши ответ в шаблон ниже: каждая строка вывода — отдельной строкой.
// Лишние пробелы и кавычки ' или " не важны — [1, 2] и [ 1, 2 ] засчитаются одинаково.
// Проверить себя:  node test.js 04/4
// Разбор — в js-lekciya-2.html, блок 4, задача 4.
// ──────────────────────────────────────────────────────────────────────

const answer = `

`;

// ─── код задачи (не меняй) ───
console.log([[1], [2, 3]].reduce((acc, x) => acc.concat(x), []));
console.log([1, 2, 3].reduce((acc, x, i) => acc + x * i, 0));
console.log([5].reduce((acc, x) => acc * 100));
console.log([1, 2, 3].reduce((acc, x) => acc + x, undefined));
try {
  [].reduce((acc, x) => acc + x);
} catch (e) {
  console.log(e.name + ': ' + e.message);
}
