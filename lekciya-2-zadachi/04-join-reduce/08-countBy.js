// ──────────────────────────────────────────────────────────────────────
// Блок 4 · Задача 8 · Средне · ★ минимум
//
// Напиши `countBy(arr, fn)` — объект «результат `fn(элемент)` → сколько раз». Через `reduce`.
//
// `countBy(['one', 'two', 'three'], w => w.length)` → `{ 3: 2, 5: 1 }`.
//
// Проверить себя:  node test.js countBy
// Решение и разбор — в js-lekciya-2.html, блок 4, задача 8.
// ──────────────────────────────────────────────────────────────────────

function countBy(arr, fn) {
  // твой код
}

// Примеры — запусти файл (node 04-join-reduce/08-countBy.js) и сравни:
console.log(countBy(['one', 'two', 'three'], w => w.length)); // ожидается: { 3: 2, 5: 1 }
console.log(countBy([1, 2, 3, 4], x => x % 2 === 0 ? 'even' : 'odd')); // ожидается: { odd: 2, even: 2 }
console.log(countBy([], x => x)); // ожидается: {}
