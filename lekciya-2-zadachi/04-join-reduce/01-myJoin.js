// ──────────────────────────────────────────────────────────────────────
// Блок 4 · Задача 1 · Легко · ★ минимум
// Правила: не больше одного цикла (forEach, map, slice, spread — это тоже циклы); нельзя вызывать: join
//
// Напиши `myJoin(arr, separator)` — копию `arr.join(separator)` 1 в 1, одним циклом, без `join`.
//
// `myJoin([1, 2, 3])` → `'1,2,3'`, `myJoin([1, null, undefined, , 'a'], '-')` → `'1----a'`.
//
// Проверить себя:  node test.js myJoin
// Решение и разбор — в js-lekciya-2.html, блок 4, задача 1.
// ──────────────────────────────────────────────────────────────────────

function myJoin(arr, separator) {
  // твой код
}

// Примеры — запусти файл (node 04-join-reduce/01-myJoin.js) и сравни:
console.log(myJoin([1, 2, 3])); // ожидается: '1,2,3'
console.log(myJoin([1, null, undefined, , 'a'], '-')); // ожидается: '1----a'
console.log(myJoin([], '-')); // ожидается: ''
