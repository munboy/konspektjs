// ──────────────────────────────────────────────────────────────────────
// Блок 4 · Задача 12 · Средне
//
// Напиши `compose(...fns)` — функцию, которая применяет функции справа налево: `compose(f, g)(x)`
// = `f(g(x))`. Через `reduceRight`.
//
// `compose(x => x + 1, x => x * 2)(5)` → `11`.
//
// Проверить себя:  node test.js compose
// Решение и разбор — в js-lekciya-2.html, блок 4, задача 12.
// ──────────────────────────────────────────────────────────────────────

function compose(...fns) {
  // твой код
}

// Примеры — запусти файл (node 04-join-reduce/12-compose.js) и сравни:
console.log(compose(x => x + 1, x => x * 2)(5)); // ожидается: 11
console.log(compose(x => x * 2, x => x + 1)(5)); // ожидается: 12
console.log(compose()(5)); // ожидается: 5
