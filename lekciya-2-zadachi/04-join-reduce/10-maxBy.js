// ──────────────────────────────────────────────────────────────────────
// Блок 4 · Задача 10 · Средне
//
// Напиши `maxBy(arr, fn)` — элемент, у которого `fn(элемент)` наибольший (при равенстве — первый).
// Пустой массив — `undefined`. Через `reduce` без начального значения.
//
// `maxBy([{ n: 'a', age: 20 }, { n: 'b', age: 30 }], u => u.age)` → `{ n: 'b', age: 30 }`.
//
// Проверить себя:  node test.js maxBy
// Решение и разбор — в js-lekciya-2.html, блок 4, задача 10.
// ──────────────────────────────────────────────────────────────────────

function maxBy(arr, fn) {
  // твой код
}

// Примеры — запусти файл (node 04-join-reduce/10-maxBy.js) и сравни:
console.log(maxBy([{ n: 'a', age: 20 }, { n: 'b', age: 30 }], u => u.age)); // ожидается: { n: 'b', age: 30 }
console.log(maxBy(['aa', 'b', 'cc'], s => s.length)); // ожидается: 'aa'
console.log(maxBy([], x => x)); // ожидается: undefined
