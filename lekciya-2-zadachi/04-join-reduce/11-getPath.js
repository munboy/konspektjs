// ──────────────────────────────────────────────────────────────────────
// Блок 4 · Задача 11 · Средне
//
// Напиши `getPath(obj, path)` — значение по пути вида `'a.b.c'`; если по дороге чего-то нет —
// `undefined`, без ошибки. Через `split` и `reduce`.
//
// `getPath({ a: { b: { c: 7 } } }, 'a.b.c')` → `7`.
//
// Проверить себя:  node test.js getPath
// Решение и разбор — в js-lekciya-2.html, блок 4, задача 11.
// ──────────────────────────────────────────────────────────────────────

function getPath(obj, path) {
  // твой код
}

// Примеры — запусти файл (node 04-join-reduce/11-getPath.js) и сравни:
console.log(getPath({ a: { b: { c: 7 } } }, 'a.b.c')); // ожидается: 7
console.log(getPath({ a: { b: null } }, 'a.b.c')); // ожидается: undefined
console.log(getPath({ list: [10, 20] }, 'list.1')); // ожидается: 20
