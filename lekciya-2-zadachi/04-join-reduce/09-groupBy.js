// ──────────────────────────────────────────────────────────────────────
// Блок 4 · Задача 9 · Средне · ★ минимум
//
// Напиши `groupBy(arr, fn)` — объект «результат `fn(элемент)` → массив элементов». Через `reduce`,
// порядок внутри групп сохраняется.
//
// `groupBy([1, 2, 3, 4], x => x % 2 === 0 ? 'even' : 'odd')` → `{ odd: [1, 3], even: [2, 4] }`.
//
// Проверить себя:  node test.js groupBy
// Решение и разбор — в js-lekciya-2.html, блок 4, задача 9.
// ──────────────────────────────────────────────────────────────────────

function groupBy(arr, fn) {
  // твой код
}

// Примеры — запусти файл (node 04-join-reduce/09-groupBy.js) и сравни:
console.log(groupBy([1, 2, 3, 4], x => x % 2 === 0 ? 'even' : 'odd')); // ожидается: { odd: [1, 3], even: [2, 4] }
console.log(groupBy(['apple', 'avocado', 'banana'], w => w[0])); // ожидается: { a: ['apple', 'avocado'], b: ['banana'] }
console.log(groupBy([], x => x)); // ожидается: {}
