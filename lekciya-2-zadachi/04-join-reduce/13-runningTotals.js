// ──────────────────────────────────────────────────────────────────────
// Блок 4 · Задача 13 · Сложно
// Правила: не больше одного цикла (forEach, map, slice, spread — это тоже циклы)
//
// Напиши `runningTotals(arr)` — массив накопленных сумм: `i`-й элемент — сумма первых `i + 1`
// чисел. Через один `reduce` (это и есть единственный цикл).
//
// `runningTotals([1, 2, 3, 4])` → `[1, 3, 6, 10]`.
//
// Проверить себя:  node test.js runningTotals
// Решение и разбор — в js-lekciya-2.html, блок 4, задача 13.
// ──────────────────────────────────────────────────────────────────────

function runningTotals(arr) {
  // твой код
}

// Примеры — запусти файл (node 04-join-reduce/13-runningTotals.js) и сравни:
console.log(runningTotals([1, 2, 3, 4])); // ожидается: [1, 3, 6, 10]
console.log(runningTotals([])); // ожидается: []
console.log(runningTotals([5, -5, 5])); // ожидается: [5, 0, 5]
