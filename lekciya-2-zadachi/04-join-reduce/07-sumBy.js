// ──────────────────────────────────────────────────────────────────────
// Блок 4 · Задача 7 · Легко
//
// Напиши `sumBy(arr, key)` — сумма поля `key` у всех объектов массива. Через `reduce`.
//
// `sumBy([{ price: 10 }, { price: 5 }], 'price')` → `15`.
//
// Проверить себя:  node test.js sumBy
// Решение и разбор — в js-lekciya-2.html, блок 4, задача 7.
// ──────────────────────────────────────────────────────────────────────

function sumBy(arr, key) {
  // твой код
}

// Примеры — запусти файл (node 04-join-reduce/07-sumBy.js) и сравни:
console.log(sumBy([{ price: 10 }, { price: 5 }], 'price')); // ожидается: 15
console.log(sumBy([], 'price')); // ожидается: 0
