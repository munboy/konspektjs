// ──────────────────────────────────────────────────────────────────────
// Блок 7 · Задача 8 · Сложно · ★ минимум
// Правила: не больше 2 циклов (forEach, map, slice, spread — это тоже циклы); без рекурсии; нельзя вызывать: JSON.parse
//
// Напиши `myParse(text)` — копию `JSON.parse(text)` 1 в 1 (без `reviver`): объекты, массивы,
// строки с экранированием, числа по грамматике JSON, `true`, `false`, `null`, пробелы между
// значениями; любая ошибка — `SyntaxError`. Без рекурсии, не больше двух циклов.
//
// Проверить себя:  node test.js myParse
// Решение и разбор — в js-lekciya-2.html, блок 7, задача 8.
// ──────────────────────────────────────────────────────────────────────

function myParse(text) {
  // твой код
}

// Примеры — запусти файл (node 07-json/08-myParse.js) и сравни:
console.log(myParse('{"a":[1,{"b":"x"}],"ok":true}')); // ожидается: { a: [1, { b: 'x' }], ok: true }
console.log(myParse(' [ 1 , -2.5e1 , true , null ] ')); // ожидается: [1, -25, true, null]
console.log(myParse('"a\\u0062c"')); // ожидается: 'abc'
