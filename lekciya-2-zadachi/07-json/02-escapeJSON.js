// ──────────────────────────────────────────────────────────────────────
// Блок 7 · Задача 2 · Средне · ★ минимум
// Правила: не больше одного цикла (forEach, map, slice, spread — это тоже циклы); нельзя вызывать: JSON.stringify
//
// Напиши `escapeJSON(str)` — строку в кавычках, как её пишет `JSON.stringify`: `"` → `\"`, `\` →
// `\\`, перевод строки, возврат каретки, табуляция, `\b`, `\f` — короткими записями, остальные
// символы с кодом меньше 32 — как `\u00XX` (шестнадцатеричный код, маленькие буквы). Один цикл.
//
// `escapeJSON('say "hi"')` → `'"say \\"hi\\""'`, то есть `"say \"hi\""`.
//
// Проверить себя:  node test.js escapeJSON
// Решение и разбор — в js-lekciya-2.html, блок 7, задача 2.
// ──────────────────────────────────────────────────────────────────────

function escapeJSON(str) {
  // твой код
}

// Примеры — запусти файл (node 07-json/02-escapeJSON.js) и сравни:
console.log(escapeJSON('say "hi"')); // ожидается: '"say \\"hi\\""'
console.log(escapeJSON('a\nb')); // ожидается: '"a\\nb"'
console.log(escapeJSON('C:\\dir')); // ожидается: '"C:\\\\dir"'
