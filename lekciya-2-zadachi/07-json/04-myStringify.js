// ──────────────────────────────────────────────────────────────────────
// Блок 7 · Задача 4 · Сложно · ★ минимум
// Правила: не больше 2 циклов (forEach, map, slice, spread — это тоже циклы); без рекурсии; нельзя вызывать: JSON.stringify
//
// Напиши `myStringify(value)` — копию `JSON.stringify(value)` 1 в 1 (без `replacer` и `space`):
// все правила из таблицы теории, `toJSON`, вложенность любой глубины, циклы и `BigInt` —
// `TypeError`. Без рекурсии, не больше двух циклов.
//
// Проверить себя:  node test.js myStringify
// Решение и разбор — в js-lekciya-2.html, блок 7, задача 4.
// ──────────────────────────────────────────────────────────────────────

function myStringify(value) {
  // твой код
}

// Примеры — запусти файл (node 07-json/04-myStringify.js) и сравни:
console.log(myStringify({ a: [1, { b: 'x' }], d: new Date(0) }));
