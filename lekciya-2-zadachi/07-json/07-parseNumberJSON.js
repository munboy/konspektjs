// ──────────────────────────────────────────────────────────────────────
// Блок 7 · Задача 7 · Средне
// Правила: не больше одного цикла (forEach, map, slice, spread — это тоже циклы); нельзя вызывать: JSON.parse
//
// Напиши `parseNumberJSON(text)` — проверить, что строка — правильное число JSON (`-?`, потом `0`
// или цифры без ведущего нуля, потом необязательные `.цифры`, потом необязательные `e`/`E`, знак и
// цифры), и вернуть его значение. Неправильное — `SyntaxError`. Один цикл: проверка — автоматом по
// символам, перевод в число — `Number()` уже проверенной строки.
//
// `parseNumberJSON('-12.5e1')` → `-125`, `parseNumberJSON('01')` → `SyntaxError`.
//
// Проверить себя:  node test.js parseNumberJSON
// Решение и разбор — в js-lekciya-2.html, блок 7, задача 7.
// ──────────────────────────────────────────────────────────────────────

function parseNumberJSON(text) {
  // твой код
}

// Примеры — запусти файл (node 07-json/07-parseNumberJSON.js) и сравни:
console.log(parseNumberJSON('-12.5e1')); // ожидается: -125
console.log(parseNumberJSON('0')); // ожидается: 0
console.log(parseNumberJSON('-0')); // ожидается: -0
