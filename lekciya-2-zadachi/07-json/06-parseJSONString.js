// ──────────────────────────────────────────────────────────────────────
// Блок 7 · Задача 6 · Средне · ★ минимум
// Правила: не больше одного цикла (forEach, map, slice, spread — это тоже циклы); нельзя вызывать: JSON.parse
//
// Напиши `parseJSONString(text)` — разобрать строку JSON вместе с кавычками: `'"a\\nb"'` →
// `'a\nb'`. Поддержать `\" \\ \/ \n \t \r \b \f` и `\uXXXX`. Нет открывающей или закрывающей
// кавычки, неизвестное экранирование, управляющий символ внутри строки или лишний текст после неё
// — `SyntaxError`. Один цикл, без `parseInt`.
//
// Проверить себя:  node test.js parseJSONString
// Решение и разбор — в js-lekciya-2.html, блок 7, задача 6.
// ──────────────────────────────────────────────────────────────────────

function parseJSONString(text) {
  // твой код
}

// Примеры — запусти файл (node 07-json/06-parseJSONString.js) и сравни:
console.log(parseJSONString('"a\\nb"')); // ожидается: 'a\nb'
console.log(parseJSONString('"a\\u0062c"')); // ожидается: 'abc'
console.log(parseJSONString('"say \\"hi\\""')); // ожидается: 'say "hi"'
