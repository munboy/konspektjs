// ──────────────────────────────────────────────────────────────────────
// Блок 6 · Задача 3 · Средне
//
// Напиши `sortBy(arr, key)` — новый массив объектов, отсортированный по полю `key` по возрастанию
// (поле — число или строка). Равные — в прежнем порядке.
//
// `sortBy([{ n: 'b', age: 30 }, { n: 'a', age: 20 }], 'age')` → сначала `a`, потом `b`.
//
// Проверить себя:  node test.js sortBy
// Решение и разбор — в js-lekciya-2.html, блок 6, задача 3.
// ──────────────────────────────────────────────────────────────────────

function sortBy(arr, key) {
  // твой код
}

// Примеры — запусти файл (node 06-sort/03-sortBy.js) и сравни:
console.log(sortBy([{ n: 'b', age: 30 }, { n: 'a', age: 20 }], 'age').map(u => u.n)); // ожидается: ['a', 'b']
console.log(sortBy([{ n: 'b' }, { n: 'a' }, { n: 'c' }], 'n').map(u => u.n)); // ожидается: ['a', 'b', 'c']
console.log(sortBy([{ n: 'x', v: 1 }, { n: 'y', v: 0 }, { n: 'z', v: 1 }], 'v').map(u => u.n)); // ожидается: ['y', 'x', 'z']
