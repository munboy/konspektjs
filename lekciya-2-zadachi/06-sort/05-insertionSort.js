// ──────────────────────────────────────────────────────────────────────
// Блок 6 · Задача 5 · Средне · ★ минимум
// Правила: не больше 2 циклов (forEach, map, slice, spread — это тоже циклы); без рекурсии; нельзя вызывать: sort, toSorted
//
// Напиши `insertionSort(arr, compare)` — сортировку вставками на месте с функцией сравнения (по
// умолчанию — числа по возрастанию). Устойчивая. Вернуть тот же массив.
//
// `insertionSort([5, 2, 4, 1])` → `[1, 2, 4, 5]`.
//
// Проверить себя:  node test.js insertionSort
// Решение и разбор — в js-lekciya-2.html, блок 6, задача 5.
// ──────────────────────────────────────────────────────────────────────

function insertionSort(arr, compare = (a, b) => a - b) {
  // твой код
}

// Примеры — запусти файл (node 06-sort/05-insertionSort.js) и сравни:
console.log(insertionSort([5, 2, 4, 1])); // ожидается: [1, 2, 4, 5]
console.log(insertionSort([])); // ожидается: []
console.log(insertionSort([3, 1, 2], (a, b) => b - a)); // ожидается: [3, 2, 1]
