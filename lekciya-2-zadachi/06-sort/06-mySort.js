// ──────────────────────────────────────────────────────────────────────
// Блок 6 · Задача 6 · Сложно · ★ минимум
// Правила: не больше 2 циклов (forEach, map, slice, spread — это тоже циклы); без рекурсии; нельзя вызывать: sort, toSorted
//
// Напиши `mySort(arr, compareFn)` — копию `arr.sort(compareFn)` 1 в 1, не больше двух циклов, без
// рекурсии:
//
// - на месте, возвращает тот же массив, устойчивая;
// - без `compareFn` — сравнение строк: `String(a)` и `String(b)`;
// - `undefined` — в конец, `compareFn` для них не вызывается; дырки — после `undefined`, и
// остаются дырками;
// - результат `compareFn`, равный `NaN`, считается нулём;
// - `compareFn` не функция и не `undefined` — `TypeError`.
//
// Проверить себя:  node test.js mySort
// Решение и разбор — в js-lekciya-2.html, блок 6, задача 6.
// ──────────────────────────────────────────────────────────────────────

function mySort(arr, compareFn) {
  // твой код
}

// Примеры — запусти файл (node 06-sort/06-mySort.js) и сравни:
console.log(mySort([10, 9, 1, 100])); // ожидается: [1, 10, 100, 9]
