// ──────────────────────────────────────────────────────────────────────
// Блок 6 · Задача 4 · Легко · ★ минимум
// Правила: не больше 2 циклов (forEach, map, slice, spread — это тоже циклы); без рекурсии; нельзя вызывать: sort, toSorted
//
// Напиши `bubbleSort(arr)` — сортировку пузырьком: числа по возрастанию, на месте, вернуть тот же
// массив. Если за проход не было ни одного обмена — массив уже отсортирован, выходим.
//
// `bubbleSort([5, 1, 4, 2])` → `[1, 2, 4, 5]`.
//
// Проверить себя:  node test.js bubbleSort
// Решение и разбор — в js-lekciya-2.html, блок 6, задача 4.
// ──────────────────────────────────────────────────────────────────────

function bubbleSort(arr) {
  // твой код
}

// Примеры — запусти файл (node 06-sort/04-bubbleSort.js) и сравни:
console.log(bubbleSort([5, 1, 4, 2])); // ожидается: [1, 2, 4, 5]
