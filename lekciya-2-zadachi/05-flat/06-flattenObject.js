// ──────────────────────────────────────────────────────────────────────
// Блок 5 · Задача 6 · Сложно
// Правила: не больше 2 циклов (forEach, map, slice, spread — это тоже циклы); без рекурсии
//
// Напиши `flattenObject(obj)` — развернуть вложенный объект в плоский с ключами-путями через
// точку. Массивы и пустые объекты не разворачивать — это значения. Порядок ключей — как при обходе
// в глубину. Без рекурсии.
//
// `flattenObject({ a: { b: 1, c: { d: 2 } }, e: 3 })` → `{ 'a.b': 1, 'a.c.d': 2, e: 3 }`.
//
// Проверить себя:  node test.js flattenObject
// Решение и разбор — в js-lekciya-2.html, блок 5, задача 6.
// ──────────────────────────────────────────────────────────────────────

function flattenObject(obj) {
  // твой код
}

// Примеры — запусти файл (node 05-flat/06-flattenObject.js) и сравни:
console.log(flattenObject({ a: { b: 1, c: { d: 2 } }, e: 3 })); // ожидается: { 'a.b': 1, 'a.c.d': 2, e: 3 }
console.log(Object.keys(flattenObject({ a: { b: 1 }, c: 2 }))); // ожидается: ['a.b', 'c']
console.log(flattenObject({ x: {}, y: [1], z: null })); // ожидается: { x: {}, y: [1], z: null }
