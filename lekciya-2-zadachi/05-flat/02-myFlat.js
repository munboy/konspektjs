// ──────────────────────────────────────────────────────────────────────
// Блок 5 · Задача 2 · Сложно · ★ минимум
// Правила: не больше 2 циклов (forEach, map, slice, spread — это тоже циклы); без рекурсии; нельзя вызывать: flat, flatMap, concat
//
// Напиши `myFlat(arr, depth)` — копию `arr.flat(depth)` 1 в 1: глубина по умолчанию 1,
// превращается в целое (`'2'` → 2, `1.7` → 1, `NaN` → 0), `Infinity` — всё; дырки выбрасываются на
// всех уровнях. Без рекурсии, не больше двух циклов.
//
// `myFlat([1, [2, [3, [4]]]], 2)` → `[1, 2, 3, [4]]`.
//
// Проверить себя:  node test.js myFlat
// Решение и разбор — в js-lekciya-2.html, блок 5, задача 2.
// ──────────────────────────────────────────────────────────────────────

function myFlat(arr, depth) {
  // твой код
}

// Примеры — запусти файл (node 05-flat/02-myFlat.js) и сравни:
console.log(myFlat([1, [2, [3, [4]]]], 2)); // ожидается: [1, 2, 3, [4]]
