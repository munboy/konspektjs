// ──────────────────────────────────────────────────────────────────────
// Блок 5 · Задача 5 · Средне
// Правила: не больше 2 циклов (forEach, map, slice, spread — это тоже циклы); нельзя вызывать: flat, flatMap, map
//
// Напиши `myFlatMap(arr, fn)` — как `arr.flatMap(fn)`: `fn(элемент, индекс, массив)`,
// результат-массив раскрывается на один уровень, не-массив добавляется как есть. Дырки исходного
// массива пропускаются. Не больше двух циклов.
//
// `myFlatMap([1, 2], x => [x, x * 10])` → `[1, 10, 2, 20]`.
//
// Проверить себя:  node test.js myFlatMap
// Решение и разбор — в js-lekciya-2.html, блок 5, задача 5.
// ──────────────────────────────────────────────────────────────────────

function myFlatMap(arr, fn) {
  // твой код
}

// Примеры — запусти файл (node 05-flat/05-myFlatMap.js) и сравни:
console.log(myFlatMap([1, 2], x => [x, x * 10])); // ожидается: [1, 10, 2, 20]
console.log(myFlatMap([1, 2, 3], x => (x === 2 ? [] : x))); // ожидается: [1, 3]
console.log(myFlatMap(['a b', 'c'], s => s.split(' '))); // ожидается: ['a', 'b', 'c']
